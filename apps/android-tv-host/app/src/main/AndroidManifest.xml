<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

  <uses-feature
    android:name="android.software.leanback"
    android:required="true" />
  <uses-feature
    android:name="android.hardware.touchscreen"
    android:required="false" />
  <uses-feature
    android:name="android.hardware.faketouch"
    android:required="false" />

  <application
    android:allowBackup="true"
    android:banner="@drawable/ic_tv_banner"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.StremioShell"
    android:usesCleartextTraffic="${usesCleartextTraffic}">
    <activity
      android:name=".PlayerActivity"
      android:exported="false"
      android:theme="@style/Theme.StremioShell.NoActionBar" />

    <!-- Native Compose TV app; becomes the launcher target once it reaches
         parity with the WebView shell for the Comet/Real-Debrid flow. -->
    <activity
      android:name=".tv.TvAppActivity"
      android:exported="true"
      android:launchMode="singleTask"
      android:theme="@style/Theme.StremioShell.NoActionBar" />

    <activity
      android:name=".tv.player.MpvPlayerActivity"
      android:exported="false"
      android:screenOrientation="landscape"
      android:supportsPictureInPicture="false"
      android:theme="@style/Theme.StremioShell.NoActionBar" />

    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:launchMode="singleTask"
      android:theme="@style/Theme.StremioShell.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />

        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <data android:scheme="stremio-shell" />
      </intent-filter>
    </activity>

    <!-- Keeps the historical launcher component name from the tv flavor so
         updates do not drop the app from TV home screens. Points at the
         native Compose app since 0.3.0; the WebView MainActivity remains
         intent-reachable as a fallback for one release. -->
    <activity-alias
      android:name=".TvLauncherActivity"
      android:enabled="true"
      android:exported="true"
      android:label="@string/app_name"
      android:targetActivity=".tv.TvAppActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
      </intent-filter>
    </activity-alias>

    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>
  </application>

</manifest>
